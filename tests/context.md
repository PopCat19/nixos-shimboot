# Context

- run-tests.sh — Runs all unit tests
- test-bwrap-workaround.sh — Tests bwrap LSM workaround functionality
- test-logging.sh — Tests logging.sh functions
- test-runtime.sh — Tests runtime.sh functions
- test-devices.sh — Tests devices.sh functions
